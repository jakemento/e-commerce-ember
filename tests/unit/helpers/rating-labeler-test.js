import { ratingLabeler } from 'e-commerce-ember/helpers/rating-labeler';
import { module, test } from 'qunit';

module('Unit | Helper | rating labeler');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ratingLabeler([42]);
  assert.ok(result);
});
